# assets/

Drop the **Buckland testimonial** video here (referenced by `index.html`, Proof section):

- `testimonial-v2.mp4` — the Buckland interview (`Testimonial - v2.mp4`, ~2.9MB) from the
  original Drive folder. Small enough to self-host.
- `buckland-testimonial-poster.jpg` — a still frame used as the video poster (prevents
  layout shift and avoids loading the video until played).

If you'd rather host the testimonial externally (YouTube/Vimeo), replace the
`<video>` block in `index.html` with the platform embed instead.

## Not here on purpose

- The **thank-you page video** (`Video 1 C.mp4`, 573MB) is **not** stored in this repo.
  It's too large to self-host or embed via Drive. Compress + self-host it, or upload to
  YouTube/Vimeo/Cloudflare Stream, then replace the placeholder in `thank-you.html`.
- The residential-tier CRM screenshots from the original Drive folder are intentionally
  **not** used anywhere — they undercut the £1M–£10M positioning.
